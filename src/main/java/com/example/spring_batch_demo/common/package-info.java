/**
 * JDK-only helpers shared across layers (no Spring/Batch/JDBC types).
 *
 * <p>Prefer domain or application types for business meaning; use this package sparingly for
 * small utilities (formatting, parsing) that truly have no better home.</p>
 */
package com.example.spring_batch_demo.common;
